import { useState } from 'react'
import React from 'react';
import ReactDOM from 'react-dom/client';

function home() {
    let texto = "Qualquer texto que eu queira aqui"
    return texto
};

export default home()